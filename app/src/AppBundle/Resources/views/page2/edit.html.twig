{% extends 'AppBundle::base.html.twig' %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                {{ form_start(form) }}
                    <div id="form_body">
                        {% include 'AppBundle:Form:form.html.twig' with {'form': form} %}
                    </div>
                    <button type="submit" class="btn btn-primary">Зберегти</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}